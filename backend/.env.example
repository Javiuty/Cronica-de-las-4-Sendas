# Solo hace falta para correr el backend fuera de docker compose
# (con `uvicorn app.main:app --reload` desde la carpeta backend/).
# En compose estas variables las pone el propio docker-compose.yml.

DATABASE_URL=postgresql+asyncpg://cronica:cronica@localhost:5432/cronica
JWT_SECRETO=
ANTHROPIC_API_KEY=
# CLAUDE_MODELO=claude-opus-5
ORIGENES_PERMITIDOS=http://localhost:5173,http://127.0.0.1:5173
